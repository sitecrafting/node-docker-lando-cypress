# Bitbucket Pipelines: Node + Lando + Cypress

Docker image for testing with Lando and Cypress using the official Node base image.

## Usage:

```bash
docker run -it sitecrafting/bitbucketpipelines-node-lando-cypress:latest
```
